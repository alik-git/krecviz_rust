fn main() {
    // This is a helper from Rerun's toolset that collects build info.
    // If you don’t have re_build_tools, you can skip or remove this line.
    // For now, we’ll leave it out to keep it super simple, or do:
    // re_build_tools::export_build_info_vars_for_crate("krecviz_rust");
}